# this war file is same as tw1.war, except that it is signed
# We don't want bnd to make the manifest
-nomanifest=true
# We have WAR layout and non-bundle manifest
-failok=true
# We don't want bnd to put the source code in the jar
-sources=false
# We don't want bnd to try and figure out what classes to put in the jar
-resourceonly

Include-Resource = \
	resources/tw1, \
	WEB-INF/classes/${replace;${p}.tw1;\\.;/}=${bin}/${replace;${p}.tw1;\\.;/}

-sign: test
