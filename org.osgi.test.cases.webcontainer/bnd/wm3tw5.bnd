# We don't want bnd to make the manifest
-nomanifest=true
# We have WAR layout and non-bundle manifest
-failok=true
# We don't want bnd to put the source code in the jar
-sources=false
# We don't want bnd to try and figure out what classes to put in the jar
-resourceonly

Include-Resource = \
	resources/tw5, \
	resources/wm3tw5, \
	WEB-INF/classes/${replace;${p}.tw5;\\.;/}=${bin}/${replace;${p}.tw5;\\.;/}, \
	WEB-INF/lib2/log.jar=log.jar, \
	WEB-INF/lib/simple.jar=simple.jar 