# Set javac settings from JDT prefs
-include: ${build}/eclipse/jdt.bnd

Export-Package: ${p}.*; -split-package:=first

-runpath =
-buildpath = ee.minimum; version=1.2.1, \
	org.osgi.framework;					version=project
