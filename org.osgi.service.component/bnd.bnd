# Set javac settings from JDT prefs
-include: ${build}/eclipse/jdt.bnd, ${build}/companion.bnd

Export-Package: !${p}.annotations, ${p}.*; -split-package:=first
Private-Package: ${p}.annotations

-buildpath = \
    osgi.annotation; version=latest, \
    osgi.core; version=6.0, \
    osgi.promise;version=6.0, \
    org.osgi.namespace.extender;version=latest, \
    org.osgi.util.converter.annotations;version=latest
