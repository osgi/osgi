Bundle-Description				: \
	OSGi Service Platform Release ${version;=;${osgi.release.version}} Version ${version;==;${osgi.release.version}}, \
	Interfaces and Classes for use in compiling bundles.

Bundle-Version					: ${osgi.release.version}
DynamicImport-Package			: *
Import-Package					: *; resolution:=optional

Export-Package					: ${spec.packages}

Include-Resource 				: ${spec.resources}
