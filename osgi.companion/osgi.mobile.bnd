Bundle-Description				: \
	OSGi Service Platform Release ${version;=;${osgi.release.version}} Version ${version;==;${mobile.release.version}}, \
	Mobile Interfaces and Classes for use in compiling bundles.

Bundle-Version					: ${mobile.release.version}
DynamicImport-Package			: *
Import-Package					: *; resolution:=optional

Export-Package					: ${mobile.packages}

Include-Resource 				: ${mobile.resources}
