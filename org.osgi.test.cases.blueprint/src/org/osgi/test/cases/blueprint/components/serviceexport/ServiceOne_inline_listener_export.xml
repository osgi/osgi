<?xml version="1.0" encoding="UTF-8"?>
<blueprint xmlns="http://www.osgi.org/xmlns/blueprint/v1.0.0">

    <description>
        This is just a simple exported service using a single interface.
    </description>


   <bean id="ServiceOneListener" class="org.osgi.test.cases.blueprint.components.serviceexport.PrototypeRegistrationStateListener">
       <argument value="ServiceOneListener"/>
       <!-- Another circular depdendency.  This allows us to check the service registration state
            during the listener calls -->
       <property name="registration" ref="ServiceOneService"/>
   </bean>

   <!-- simple export using a reference and just one interface. -->
   <service id="ServiceOneService" interface="org.osgi.test.cases.blueprint.services.TestServiceOne">
       <service-properties>
           <entry key="service.component.name" value="ServiceOneService" />
       </service-properties>
       <registration-listener ref="ServiceOneListener"
           registration-method="registered"
           unregistration-method="unregistered"/>
       <!-- the node this service export maps into. -->
      <bean class="org.osgi.test.cases.blueprint.services.TestGoodServiceSubclass">
          <argument value="ServiceOne"/>
      </bean>
   </service>
</blueprint>

