<?xml version="1.0" encoding="UTF-8"?>
<blueprint xmlns="http://www.osgi.org/xmlns/blueprint/v1.0.0"
    default-activation="eager">

    <description>
        Cycle-breaking example shown in section 121.2.6 of the blueprint spec.  The spec
        says that cycles involving prototype scope beans must be detected and broken rather
        that causing an infinite loop.  This is the same as the singleton test, but with a
        prototype scope bean.
    </description>

    <bean id="a" class="org.osgi.test.cases.blueprint.components.injection.CircularComponentInjection" init-method="init" scope="prototype">
       <argument value="a"/>
       <property name="componentOne" ref="a"/>
   </bean>
</blueprint>

