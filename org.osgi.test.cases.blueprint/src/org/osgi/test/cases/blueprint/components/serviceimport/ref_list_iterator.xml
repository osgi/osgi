<?xml version="1.0" encoding="UTF-8"?>
<blueprint xmlns="http://www.osgi.org/xmlns/blueprint/v1.0.0">

    <description>
        This is a checker for reference list iterator behavior
    </description>

    <!-- NB:  this also tests the ability for a collection to be a top-level component -->
    <ref-list id="TestCollection" interface="org.osgi.test.cases.blueprint.services.TestServiceOne" availability="optional">
        <listener bind-method="bind" unbind-method="unbind" ref="ServiceOneListener"/>
    </ref-list>

    <!-- This gets both a ServiceManager and a reference collection injected -->
   <bean id="ReferenceChecker" class="org.osgi.test.cases.blueprint.components.serviceimport.ListIteratorChecker" init-method="init">
       <argument value="ReferenceChecker"/>
       <property name="serviceManager">
           <reference interface="org.osgi.test.cases.blueprint.services.ServiceManager"/>
       </property>
       <property name="list" ref="TestCollection" />
   </bean>
</blueprint>

