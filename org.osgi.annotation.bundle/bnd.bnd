# Set javac settings from JDT prefs
-include: ${includes}/jdt.bnd, ${includes}/companion.bnd, ${includes}/annotation.bnd

Export-Package: ${p}.*; -split-package:=first

-buildpath = \
  org.osgi.annotation.versioning; version=latest
