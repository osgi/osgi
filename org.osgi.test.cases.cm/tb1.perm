# Lines beginning with '#' or '//' are comments
#
# This file contains the permissions to be granted
# to cm.tb1 test bundle.
# The permissions are listed one per
# line in PermissionInfo encoded format.
# See org.osgi.service.permissionadmin.PermissionInfo

${permissions;packages}
#the line below is intentionally commented out so we test if the framework
#prevents the bundle from registering the configuration listener
#(org.osgi.framework.ServicePermission "ConfigurationListener" "register")

