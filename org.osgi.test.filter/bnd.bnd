# Set javac settings from JDT prefs
-include: ${build}/eclipse/jdt.bnd

Bundle-Activator: 		${p}.Activator
Private-Package: 		${p}.*

Bundle-Description: 	OSGi Test Filter. Filters out services that are not meant to be used by the CT.

-buildpath = \
	osgi.core;				version=4.3.1
