-include            : layout.bnd
-dependson          : osgi.companion, ${build.impls}
-resourceonly       : true

-outputmask = ${@bsn}-${version;===;${@version}}.jar

# Build the osgi spec files
-sub       = !layout.bnd, *.bnd
